<Project Sdk="Microsoft.NET.Sdk.Web">

    <ItemGroup>
        <PackageReference Include="Grpc.AspNetCore" />
        <PackageReference Include="Microsoft.AspNetCore.OpenApi" />
    </ItemGroup>

    <ItemGroup>
      <!-- Server half only: Events implements this contract, it does not call it. -->
      <Protobuf Include="..\TicketMaster.Common\Protos\events.proto"
                ProtoRoot="..\TicketMaster.Common\Protos"
                GrpcServices="Server" />
    </ItemGroup>

    <ItemGroup>
      <!-- The exception-to-status mapping is internal but load-bearing for the API contract. -->
      <InternalsVisibleTo Include="EventsApi" />
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\Events.Application\Events.Application.csproj" />
      <!-- The composition root is the one place allowed to know which store is in use: Application
           stops at the repository interfaces, so the host has to supply the implementations. -->
      <ProjectReference Include="..\Events.Cosmos\Events.Cosmos.csproj" />
    </ItemGroup>

</Project>

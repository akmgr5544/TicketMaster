<Project Sdk="Microsoft.NET.Sdk">

    <!-- Intentionally free of package and project references. The domain owns its own types; the
         persistence layer maps them. Guarded by EventsArchitecture.Tests.DependenceTest. -->

</Project>
